name: melos_example
description: Example for melos packages manager applying monorepo concept.
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

workspace:

dependencies:

dev_dependencies:
  melos: ^^7.0.0-dev.9

melos:
# Setup scripts that will be run across all packages.